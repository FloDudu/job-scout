[project]
name = "job-scout"
version = "0.1.0"
description = "Automated job offer triage pipeline using the Claude API (RAG + semantic dedup)"
requires-python = ">=3.10"
dependencies = [
    "anthropic",
    "python-dotenv",
]

[tool.setuptools]
package-dir = {"" = "src"}
